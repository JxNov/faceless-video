export const ROUTES = {
  HOME: '/',
  AUTH_LOGIN: '/login',
  AUTH_LOGOUT: '/logout',
  AUTH_REGISTER: '/register',
  USER: '/user',
};

export const PUBLIC_ROUTES: string[] = [ROUTES.AUTH_LOGIN, ROUTES.AUTH_REGISTER];
